interface:
  display_name: "Scale Agent Pool"
  short_description: "Run large bounded programmatic subagent batches"
  default_prompt: "Process these independent items through a configurable bounded subagent pool and return structured results."
