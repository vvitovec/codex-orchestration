interface:
  display_name: "Scale Agent Pool"
  short_description: "Run large adaptive programmatic subagent batches"
  default_prompt: "Process these independent items through a configurable adaptive subagent pool and return structured results."
